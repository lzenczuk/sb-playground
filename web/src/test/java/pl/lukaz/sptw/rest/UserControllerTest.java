package pl.lukaz.sptw.rest;

import org.junit.Test;
import org.springframework.security.authentication.AuthenticationManager;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;

import static org.mockito.Mockito.*;

/**
 * @author lzenczuk 04/08/2015
 */
public class UserControllerTest {

    /*@Test
    public void correctLogin(){
        UserController userController = new UserController();

        AuthenticationManager authenticationManagerMock = mock(AuthenticationManager.class);
        when(authenticationManagerMock.authenticate(new UsernamePasswordAuthenticationToken("testUser", "password"))).thenReturn(new UsernamePasswordAuthenticationToken())
        userController.authenticationManager=authenticationManagerMock;
    }*/
}
